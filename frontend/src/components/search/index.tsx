import { Input } from "@chakra-ui/react";

// TODO: search can also stay in place on scroll, when hit a certain point (top of browser)
const Search = () => {
  return <Input placeholder="Search ..." size="lg" borderRadius="lg" />;
};

export { Search };
